% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_focus_components.R
\name{estimate_focus_components_iid}
\alias{estimate_focus_components_iid}
\title{Estimate components for \code{\link[=focus_engine]{focus_engine()}} under iid sampling}
\usage{
estimate_focus_components_iid(
  theta,
  n,
  loglik,
  score = NULL,
  information = NULL,
  simulate,
  nsim = 1000,
  parallelize = FALSE,
  diagnostics = FALSE,
  ...
)
}
\arguments{
\item{theta}{Numeric parameter vector at which the components are
estimated.}

\item{n}{Integer. The intended sample size of the iid dataset for
which the components are required.}

\item{loglik}{A function returning the log-likelihood contribution
of a single observation evaluated at a supplied parameter
vector. It is expected to have an interface of the form
\code{loglik(theta, data, ...)} and to return a single numeric
value.}

\item{score}{Optional function returning the score contribution of a
single observation. It is expected to have an interface of the
form \code{score(theta, data, ...)}. If \code{NULL} (default), the score
contribution is computed numerically from \code{loglik}.}

\item{information}{Optional function returning the observed
information contribution of a single observation, namely the
negative Hessian contribution of the log-likelihood. It is
expected to have an interface of the form \code{information(theta, data, ...)}. If \code{NULL} (default), the information contribution
is computed numerically from \code{loglik}, or, if \code{score} is
supplied, as the negative Jacobian of \code{score}.}

\item{simulate}{A function that simulates one iid observation at the
supplied parameter vector \code{theta}. It is expected to have an
interface of the form \code{simulate(theta, ...)} and to return one
simulated observation at each call.}

\item{nsim}{Integer. The number of simulated iid observations used
to estimate the components.}

\item{parallelize}{Logical. If \code{TRUE}, use
\code{\link[future.apply:future_lapply]{future.apply::future_lapply()}} for the repeated derivative
calculations. This requires the suggested package
\pkg{future.apply}.}

\item{diagnostics}{Logical. If \code{TRUE}, return simple Monte Carlo
diagnostics for the estimated components.}

\item{...}{Additional arguments passed to \code{simulate}, \code{loglik}, and,
when supplied, to \code{score} and \code{information}.}
}
\value{
An object of class \code{"focus_components"}, suitable for the
\code{components} argument of \code{\link[=focus_engine]{focus_engine()}}, with elements \code{V},
\code{P}, and \code{Q}. If \code{diagnostics = TRUE}, an additional element
\code{diagnostics} is included.
}
\description{
Estimate the model-side components used by \code{\link[=focus_engine]{focus_engine()}} by
simulating single iid observations and exploiting the additive
structure of the score and information.
}
\details{
The supplied \code{simulate} function is called repeatedly as
\code{simulate(theta, ...)}, once for each simulated iid observation.

The supplied \code{loglik} function is then used on each simulated
observation through calls of the form \code{loglik(theta, data = data, ...)}.

If both \code{score} and \code{information} are \code{NULL}, then both quantities
are obtained numerically from \code{loglik}.

If \code{score} is supplied but \code{information} is \code{NULL}, then the
simulated score contributions are computed using \code{score}, and the
simulated information contributions are computed as the negative
Jacobian of \code{score}.

If \code{information} is supplied, it is used directly, regardless of
whether \code{score} is also supplied.

The returned \code{V}, \code{P}, and \code{Q} target the corresponding full-sample
components under iid sampling by multiplying the simulated
one-observation quantities by \code{n}.
}
\seealso{
\code{\link[=focus_engine]{focus_engine()}}, \code{\link[=estimate_focus_components]{estimate_focus_components()}}
}
