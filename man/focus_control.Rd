% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/focus.R
\name{focus_control}
\alias{focus_control}
\title{Configure \code{\link[=focus]{focus()}} inference options}
\usage{
focus_control(
  correction = "median",
  ci = "wald",
  alpha = 0.05,
  Delta = 0,
  randomize = TRUE,
  check_statistic = TRUE
)
}
\arguments{
\item{correction}{Character string specifying the bias correction method.
One of:
\describe{
\item{\code{"no"}}{\code{on} in \code{\link[=focus]{focus()}} is simply evaluated at the estimates from the supplied object.}
\item{\code{"mean"}}{explicit reduced-mean-bias estimation of the \code{on} parameter.}
\item{\code{"median"}}{default; explicit reduced-median-bias estimation of the \code{on} parameter.}
}}

\item{ci}{Character string specifying the confidence interval method.
One of:
\describe{
\item{\code{"wald"}}{Wald-type confidence interval.}
\item{\code{"hulc"}}{HulC confidence interval (see \code{\link[=hulc_ci]{hulc_ci()}}).}
}}

\item{alpha}{Numeric scalar in \verb{(0, 1)}. Target miscoverage level.}

\item{Delta}{Numeric scalar in \verb{[0, 1/2)}. Median bias used by the
HulC method; see \code{\link[=hulc_ci]{hulc_ci()}}. Ignored unless \code{ci = "hulc"}.}

\item{randomize}{Logical. Whether to use randomized HulC intervals; see \code{\link[=hulc_ci]{hulc_ci()}}.
Ignored unless \code{ci = "hulc"}.}

\item{check_statistic}{Logical. Whether to validate the statistic on the
smallest partition when using HulC; see \code{\link[=hulc_ci]{hulc_ci()}}.. Ignored unless \code{ci = "hulc"}.}
}
\value{
A named list with components:
\describe{
\item{\code{correction}}{Selected bias correction method.}
\item{\code{ci}}{Selected confidence interval method.}
\item{\code{alpha}}{Miscoverage level.}
\item{\code{Delta}}{Median bias of the estimator of \code{on}.}
\item{\code{randomize}}{Logical flag for randomized HulC.}
\item{\code{check_statistic}}{Logical flag controlling statistic validation.}
}
}
\description{
Create a control object specifying bias correction and confidence interval
construction options for \code{\link[=focus]{focus()}} inference procedures.
}
\details{
This function standardizes user inputs and returns a list of settings that
can be passed to downstream estimation or inference routines.

This function performs input validation and matches arguments using
\code{match.arg()}. The returned object is intended to be passed to the
\code{control} argument of the \code{\link[=focus]{focus()}}, and controls estimation and
inference behavior.

Parameters \code{Delta}, \code{randomize}, and \code{check_statistic} are only
relevant when \code{ci = "hulc"}, but are included in the returned
object for convenience.
}
\examples{
focus_control()

focus_control(
  correction = "mean",
  ci = "hulc",
  alpha = 0.1,
  Delta = 0.05
)

}
\seealso{
\code{\link[=focus]{focus()}} \code{\link[=hulc_ci]{hulc_ci()}}
}
