% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/focus_se.R
\name{focus_se}
\alias{focus_se}
\alias{focus_se.focus_list_glm}
\alias{focus_se.focus_engine_list}
\title{Compute standard errors for focus objects}
\usage{
focus_se(object, control = list(), ...)

\method{focus_se}{focus_list_glm}(object, control = list(), ...)

\method{focus_se}{focus_engine_list}(object, V_function, control = list(), ...)
}
\arguments{
\item{object}{An object returned by \code{\link[=focus]{focus()}}.}

\item{control}{A list of control parameters used by methods; see
\code{\link[=focus_se_control]{focus_se_control()}}.}

\item{...}{Additional arguments passed to methods.}

\item{V_function}{Function returning the covariance matrix at a supplied
model parameter vector. Required for \code{focus_engine_list} objects.}
}
\value{
For the \code{focus_list_glm} method, a list with components:
\describe{
\item{\code{se}}{Numeric scalar, the delta-method standard error.}
\item{\code{theta}}{Numeric vector, the reconstructed model parameter vector.}
\item{\code{V}}{Covariance matrix evaluated at \code{theta}.}
\item{\code{gradient}}{Gradient of the focus function evaluated at \code{theta}.}
\item{\code{replace}}{Integer index of the parameter coordinate replaced when
reconstructing \code{theta}.}
}
}
\description{
Compute a delta-method standard error for a focus estimate after
reconstructing the model parameter vector at which the standard error is
evaluated.
}
\details{
For \code{focus_list_glm} and \code{focus_engine_list} objects, the method first
obtains focused estimates for all model parameters using \code{\link[=focus_on_all]{focus_on_all()}}.
It then selects an active coordinate of the original focus function,
replaces that coordinate so that the focus function matches the reported
focus estimate, evaluates the model information at the reconstructed
parameter vector, and applies the delta method. The \code{focus_engine_list}
method requires a \code{V_function} function.

The current \code{focus_list_glm} and \code{focus_engine_list} methods recognize the
following \code{control} entries:
\describe{
\item{\code{tol_deriv}}{Numeric scalar. Tolerance used to decide whether a
derivative is large enough for the corresponding parameter to be treated
as active.}
\item{\code{tol_opt}}{Numeric scalar. Tolerance for the one-dimensional
reconstruction of the replaced parameter coordinate. The optimization
objective is the squared reconstruction error divided by
\code{max(se, 1e-02)^2}, where \code{se} is the standard error stored in \code{object},
so \code{tol_opt} is applied to the corresponding unitless absolute error.}
}
}
\seealso{
\code{\link[=focus]{focus()}}, \code{\link[=focus_on_all]{focus_on_all()}}
}
