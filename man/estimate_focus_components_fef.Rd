% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_focus_components.R
\name{estimate_focus_components_fef}
\alias{estimate_focus_components_fef}
\title{Estimate components for \code{\link[=focus_engine]{focus_engine()}} in a full exponential family}
\usage{
estimate_focus_components_fef(
  theta,
  data,
  loglik,
  score = NULL,
  information = NULL,
  simulate,
  nsim = 1000,
  parallelize = FALSE,
  diagnostics = FALSE,
  ...
)
}
\arguments{
\item{theta}{Numeric parameter vector at which the components are
estimated.}

\item{data}{Observed dataset at which the observed information is
evaluated.}

\item{loglik}{A function returning the log-likelihood evaluated at
a supplied parameter vector and dataset. It is expected to have
an interface of the form \code{loglik(theta, data, ...)} and to
return a single numeric value.}

\item{score}{Optional function returning the gradient of the
log-likelihood at a supplied parameter vector and dataset. It
is expected to have an interface of the form \code{score(theta, data, ...)}. If \code{NULL} (default), the score is computed
numerically from \code{loglik}.}

\item{information}{Optional function returning the observed
information matrix, namely the negative Hessian of the
log-likelihood, at a supplied parameter vector and dataset. It
is expected to have an interface of the form
\code{information(theta, data, ...)}. If \code{NULL} (default), the
information matrix is computed numerically from \code{loglik}, or,
if \code{score} is supplied, as the negative Jacobian of \code{score}.}

\item{simulate}{A function that simulates one dataset at the
supplied parameter vector \code{theta}. It is expected to have an
interface of the form \code{simulate(theta, ...)} and to return one
simulated dataset at each call.}

\item{nsim}{Integer. The number of simulated datasets used to
estimate \code{P}.}

\item{parallelize}{Logical. If \code{TRUE}, use
\code{\link[future.apply:future_lapply]{future.apply::future_lapply()}} for the repeated score
calculations. This requires the suggested package
\pkg{future.apply}.}

\item{diagnostics}{Logical. If \code{TRUE}, return simple Monte Carlo
diagnostics for the estimated components.}

\item{...}{Additional arguments passed to \code{simulate}, \code{loglik},
and, when supplied, to \code{score} and \code{information}.}
}
\value{
An object of class \code{"focus_components"}, suitable for the
\code{components} argument of \code{\link[=focus_engine]{focus_engine()}}, with elements \code{V},
\code{P}, and \code{Q}. The returned \code{Q} consists of zero matrices. If
\code{diagnostics = TRUE}, an additional element \code{diagnostics} is
included.
}
\description{
Estimate the model-side components used by \code{\link[=focus_engine]{focus_engine()}} in the
special case where the reference parameterization is that of a full
exponential family, so that \code{Q = 0} and \code{V} can be obtained directly
from the observed information evaluated at the supplied estimate.
}
\details{
This helper assumes that the supplied parameterization is that of a
full exponential family. In that case, \code{Q} is taken to be zero and
\code{V} is obtained by inverting the observed information evaluated at
the supplied \code{theta} and \code{data}. Only \code{P} is estimated by Monte
Carlo simulation.
}
\seealso{
\code{\link[=focus_engine]{focus_engine()}}, \code{\link[=estimate_focus_components]{estimate_focus_components()}}
}
