% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_focus_components.R
\name{estimate_focus_components}
\alias{estimate_focus_components}
\title{Estimate components for \code{\link[=focus_engine]{focus_engine()}}}
\usage{
estimate_focus_components(
  theta,
  loglik,
  score = NULL,
  information = NULL,
  simulate,
  nsim = 1000,
  parallelize = FALSE,
  ...
)
}
\arguments{
\item{theta}{Numeric parameter vector at which the components are
estimated.}

\item{loglik}{A function returning the log-likelihood evaluated at
a supplied parameter vector and dataset. It is expected to have
an interface of the form \code{loglik(theta, data, ...)} and to
return a single numeric value.}

\item{score}{Optional function returning the gradient of the
log-likelihood at a supplied parameter vector and dataset. It
is expected to have an interface of the form \code{score(theta, data, ...)}. If \code{NULL} (default), the score is computed
numerically from \code{loglik}.}

\item{information}{Optional function returning the observed
information matrix, namely the negative Hessian of the
log-likelihood, at a supplied parameter vector and dataset. It
is expected to have an interface of the form
\code{information(theta, data, ...)}. If \code{NULL} (default), the
information matrix is computed numerically from \code{loglik}, or,
if \code{score} is supplied, as the negative Jacobian of \code{score}.}

\item{simulate}{A function that simulates one dataset at the
supplied parameter vector \code{theta}. It is expected to have an
interface of the form \code{simulate(theta, ...)} and to return one
simulated dataset at each call.}

\item{nsim}{Integer. The number of simulated datasets used to
estimate the components.}

\item{parallelize}{Logical. If \code{TRUE}, use
\code{\link[future.apply:future_lapply]{future.apply::future_lapply()}} for the repeated derivative
calculations. This requires the suggested package
\pkg{future.apply}.}

\item{...}{Additional arguments passed to \code{simulate}, \code{loglik},
and, when supplied, to \code{score} and \code{information}.}
}
\value{
A named list suitable for the \code{components} argument of
\code{\link[=focus_engine]{focus_engine()}}, with elements \code{V}, \code{P}, and \code{Q}.
}
\description{
Estimate the model-side components used by \code{\link[=focus_engine]{focus_engine()}} through
repeated simulation at a supplied parameter vector.
}
\details{
The supplied \code{simulate} function is called repeatedly as
\code{simulate(theta, ...)}, once for each simulated dataset.

The supplied \code{loglik} function is then used on each simulated dataset
through calls of the form \code{loglik(theta, data = data, ...)}.

If both \code{score} and \code{information} are \code{NULL}, then both quantities
are obtained numerically from \code{loglik}.

If \code{score} is supplied but \code{information} is \code{NULL}, then the
simulated score contributions are computed using \code{score}, and the
simulated information matrices are computed as the negative
Jacobian of \code{score}.

If \code{information} is supplied, it is used directly, regardless of
whether \code{score} is also supplied.

The returned \code{V} is obtained by averaging the observed information
matrices across the simulated datasets and inverting the result.

The returned \code{P} and \code{Q} are estimated by averaging the
corresponding simulation-based quantities across the simulated
datasets.
}
\seealso{
\code{\link[=focus_engine]{focus_engine()}}
}
