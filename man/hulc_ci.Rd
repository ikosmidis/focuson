% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hulc.R
\name{hulc_ci}
\alias{hulc_ci}
\title{Construct a HulC confidence interval}
\usage{
hulc_ci(
  data,
  statistic,
  level = 0.95,
  Delta = 0,
  randomize = TRUE,
  check_statistic = TRUE,
  ...
)
}
\arguments{
\item{data}{A \code{\link{data.frame}} object with observations in rows.}

\item{statistic}{A function that takes a \code{\link{data.frame}} as its first argument
and returns a single numeric value.}

\item{level}{Numeric scalar in \verb{(0, 1)}. Target coverage level.}

\item{Delta}{Numeric scalar in \verb{[0, 1/2)}. The median bias of the \code{statistic}.}

\item{randomize}{Logical. If \code{TRUE} (default), randomize between \code{B} and \code{B - 1} to
obtain less conservative finite-sample coverage.}

\item{check_statistic}{Logical. If \code{TRUE}, first evaluate \code{statistic} on the
smallest partition to check whether it can be computed on a minimal subset.}

\item{...}{Additional arguments passed to \code{statistic}.}
}
\value{
A numeric vector of length 2 with elements \code{"lower"} and \code{"upper"}.

The return value has the following attributes:
\describe{
\item{\code{Delta}}{The supplied median bias.}
\item{\code{level}}{The supplied target coverage level.}
\item{\code{B}}{The number of partitions used.}
\item{\code{error}}{\code{NA} on success, or the captured error message if
evaluating \code{statistic} failed.}
\item{\code{type}}{Character string \code{"hulc"}.}
}

If the statistic cannot be computed on the smallest partition, or fails on
one of the partitions, the function returns \code{c(NA, NA)}.
}
\description{
Compute a HulC confidence interval for a user-supplied statistic by
partitioning the data into \code{B} subsets, evaluating the statistic on each
subset, and taking the range of the resulting values.
}
\details{
The number of partitions is chosen to achieve nominal coverage
level \code{level} under the HulC construction, optionally with
randomization as described in the original method.

The observations are first randomly permuted, then split into \code{B} roughly
equal-sized partitions. The user-supplied \code{statistic} is evaluated on each
partition, and the confidence interval is defined as the range of these
values.

When \code{randomize = TRUE}, the function may reduce the number of
partitions from \code{B} to \code{B - 1} with a probability chosen to match
the randomized HulC construction (see Kuchibhotla et al, 2024,
Section 2.1).

If \code{check_statistic = TRUE} and the statistic cannot be evaluated on the
smallest partition, the function issues a warning and returns \code{c(NA, NA)}.
If evaluation instead fails later when applying \code{statistic} across all
partitions, the function also issues a warning, returns \code{c(NA, NA)}, and
stores the captured error message in the \code{"error"} attribute.

The function assumes that \code{statistic} returns a single numeric value for each
subset. If \code{statistic} returns \code{NA}, throws an error, or is undefined for
small subsets, the function returns \code{c(NA, NA)} for the interval.
}
\examples{
set.seed(1)
x <- data.frame(y = rnorm(100))

hulc_ci(
  data = x,
  statistic = function(d) mean(d$y),
  level = 0.95
)

hulc_ci(
  data = x,
  statistic = function(d, trim = 0.1) mean(d$y, trim = trim),
  level = 0.90,
  trim = 0.2
)

}
\references{
Kuchibhotla A K, Balakrishnan S, Wasserman L (2024). The HulC:
confidence regions from convex hulls. \emph{Journal of the Royal
Statistical Society Series B: Statistical Methodology}, \strong{86},
586-622. \doi{10.1093/jrsssb/qkad134}.
}
