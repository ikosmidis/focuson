% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modified_profile.R
\name{modified_profile.focus_list}
\alias{modified_profile.focus_list}
\title{Modified profile likelihood for a fitted-model focus object}
\usage{
\method{modified_profile}{focus_list}(
  fitted,
  nsim = 1000,
  parallelize = FALSE,
  grid_size = 20,
  max_level = 0.995,
  nleqslv_args = list(),
  approach = c("VM", "focus_grid"),
  focus_range = NULL,
  auglag_args = list(),
  ...
)
}
\arguments{
\item{fitted}{An object inheriting from \code{"focus_list"}, as returned by
\code{\link[=focus]{focus()}}.}

\item{nsim}{Integer. Number of common simulated datasets used to estimate
the sample-space covariance quantities.}

\item{parallelize}{Logical. If \code{TRUE}, use
\code{\link[future.apply:future_lapply]{future.apply::future_lapply()}} for the simulation calculations. This
requires the suggested package \pkg{future.apply}.}

\item{grid_size}{Number of profile points computed on each side of the
maximum likelihood estimate. Must be at least 2. For the VM approach,
the outermost point provides one grid step of plotting space beyond
\code{max_level}. For a focus-grid profile whose range does not contain the
focus at the MLE, \code{grid_size + 1} points are computed across the range.}

\item{max_level}{Nominal likelihood-ratio level in \verb{(0, 1)} whose
likelihood roots are placed one grid step inside the outer boundary of
the computed profile. Used only when \code{approach = "VM"}.}

\item{nleqslv_args}{List of additional arguments passed to
\code{\link[nleqslv:nleqslv]{nleqslv::nleqslv()}}. The \code{x}, \code{fn}, and \code{jac} arguments are
determined internally.}

\item{approach}{Character. \code{"VM"} uses the endpoint equations of Venzon and
Moolgavkar (1988) on a likelihood-root grid. \code{"focus_grid"} maximizes the
likelihood at fixed focus values.}

\item{focus_range}{Optional numeric vector containing the lower and upper
focus values. It is required when \code{approach = "focus_grid"} and must
contain two distinct finite values. It is ignored otherwise.}

\item{auglag_args}{Named list of additional arguments passed to
\code{\link[alabama:auglag]{alabama::auglag()}} when \code{approach = "focus_grid"}. The arguments \code{par},
\code{fn}, \code{gr}, \code{hin}, \code{hin.jac}, \code{heq}, and \code{heq.jac} are determined
internally. The argument is ignored when \code{approach = "VM"}.}

\item{...}{Currently unused.}
}
\value{
An object of class \code{"modified_profile_focus_list"}, inheriting from
\code{"profile_focus_list"}. See \code{\link[=modified_profile_focus]{modified_profile_focus()}} for its columns
and attributes.
}
\description{
Compute a simulation-based modified profile likelihood for the scalar
parameter defined by a fitted-model focus object.
}
\details{
If the fitted model stored in \code{fitted} is not an ML fit, it is refitted by
maximum likelihood. Likelihood quantities and simulation under the fitted
design are obtained from \code{\link[enrichwith:get_auxiliary_functions]{enrichwith::get_auxiliary_functions()}} for
supported fitted-model classes. Simulated responses replace the observed
response when the likelihood quantities are evaluated for the higher-order
adjustments. Currently, GLM and beta-regression focus objects are
supported. Results from \code{\link[=focus_engine]{focus_engine()}} are not supported.
}
\examples{

\dontrun{

data("babies", package = "cond")
babies_fit <- glm(cbind(r1, r2) ~ day + lull - 1, data = babies,
                  family = binomial)

lullyes_ind = match("lullyes", names(coef(babies_fit)))
babies_focus <- focus(babies_fit, on = function(theta) theta[lullyes_ind])
set.seed(2020)
babies_mpl <- modified_profile(babies_focus)

opar <- par(no.readonly = TRUE)
par(mfrow = c(1, 2))
plot(babies_mpl, what = "pl", ci = TRUE)
plot(babies_mpl, what = "mpl", ci = TRUE)
par(opar)

par(mfrow = c(1, 3))
plot(babies_mpl, what = "pl", signed = TRUE, ci = TRUE)
plot(babies_mpl, what = "mpl", signed = TRUE, ci = TRUE)
plot(babies_mpl, what = "rstar", ci = TRUE)
par(opar)

}

}
\seealso{
\code{\link[=modified_profile_focus]{modified_profile_focus()}}, \code{\link[=profile.focus_list]{profile.focus_list()}},
\code{\link[=plot.profile_focus_list]{plot.profile_focus_list()}}, \code{\link[=confint.profile_focus_list]{confint.profile_focus_list()}}
}
