% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confint.R
\name{confint.profile_focus_list}
\alias{confint.profile_focus_list}
\title{Confidence intervals from a focus profile}
\usage{
\method{confint}{profile_focus_list}(
  object,
  parm,
  level = 0.95,
  method = c("pl", "mpl", "rstar"),
  interpolation = c("linear", "cubic"),
  ...
)
}
\arguments{
\item{object}{An object of class \code{"profile_focus_list"}, as returned by
\code{\link[=profile_focus]{profile_focus()}}, \code{\link[=modified_profile_focus]{modified_profile_focus()}}, or
\code{\link[=profile.focus_list_glm]{profile.focus_list_glm()}}.}

\item{parm}{Currently unused.}

\item{level}{Nominal confidence level.}

\item{method}{Character. The likelihood-based method used to construct the
interval. \code{"pl"} (default) uses the ordinary profile likelihood,
\code{"mpl"} uses the modified profile likelihood, and \code{"rstar"} uses the
modified signed likelihood root.}

\item{interpolation}{Character. Interpolation method used between computed
profile points. \code{"linear"} (default) uses \code{\link[stats:approxfun]{stats::approxfun()}} and
\code{"cubic"} uses \code{\link[stats:splinefun]{stats::splinefun()}}.}

\item{...}{Currently unused.}
}
\value{
A numeric vector of length 2 with names \code{"lower"} and \code{"upper"}. The
\code{"level"}, \code{"type"}, and \code{"interpolation"} attributes record the nominal
level, interval type (\code{"pl"}, \code{"mpl"}, or \code{"rstar"}), and interpolation
method, respectively.
}
\description{
Extract a confidence interval from a computed focus profile by
interpolation on the signed likelihood-root scale.
}
\details{
This method interpolates the supplied profile and does not perform further
likelihood evaluations. The profile must extend beyond the likelihood-root
cutoffs for \code{level} in both directions on the selected root scale.
Consequently, a two-sided interval cannot be extracted from a single-branch
profile.

Methods \code{"mpl"} and \code{"rstar"} require an object returned by
\code{\link[=modified_profile_focus]{modified_profile_focus()}}. For \code{"mpl"}, the modified profile is recentered
at the maximum of its cubic-spline interpolant before constructing the
signed likelihood root. The \code{interpolation} argument then determines how
that root is interpolated to obtain the confidence limits. The modified
profile maximum must be in the interior of the supplied grid.

In contrast, \code{confint(focus_object, method = "pl")} uses \code{\link[=profile_ci]{profile_ci()}}
to solve the endpoint equations directly.
}
\seealso{
\code{\link[=profile_focus]{profile_focus()}}, \code{\link[=profile.focus_list_glm]{profile.focus_list_glm()}},
\code{\link[=modified_profile_focus]{modified_profile_focus()}}, \code{\link[=plot.profile_focus_list]{plot.profile_focus_list()}}, \code{\link[=profile_ci]{profile_ci()}}
}
