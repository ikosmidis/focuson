% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confint.R
\name{confint.focus_list}
\alias{confint.focus_list}
\title{Confidence intervals for focus objects}
\usage{
\method{confint}{focus_list}(
  object,
  parm,
  level = 0.95,
  method = "wald",
  se_at = "supplied",
  V_function = NULL,
  se_control = list(),
  nleqslv_args = list(),
  ...
)
}
\arguments{
\item{object}{An object of class \code{"focus_list"}.}

\item{parm}{Currently unused.}

\item{level}{Confidence level.}

\item{method}{Character string specifying the confidence interval
method. One of \code{"wald"}, \code{"pl"}, or \code{"hulc"}. \code{"pl"} denotes
profile-likelihood inference.}

\item{se_at}{Character string specifying where the delta-method
standard error is evaluated for \code{method = "wald"}. \code{"supplied"}
uses the standard error stored in \code{object}; \code{"compatible"}
tries to evaluate the standard error at a reconstructed model
parameter vector compatible with the reported focus estimate.}

\item{V_function}{Optional function returning the covariance matrix
at a supplied model parameter vector. Required for
\code{focus_engine_list} objects when \code{se_at = "compatible"}.}

\item{se_control}{A list of control parameters passed to
\code{\link[=focus_se]{focus_se()}} when \code{se_at = "compatible"}.}

\item{nleqslv_args}{A list of control arguments passed to
\code{\link[nleqslv:nleqslv]{nleqslv::nleqslv()}} through \code{\link[=profile_ci]{profile_ci()}} when
\code{method = "pl"}.}

\item{...}{Additional arguments for the confidence interval
method. For \code{method = "hulc"}, these are passed to \code{\link[=hulc_ci]{hulc_ci()}},
except that the nominal level is determined by \code{level}. For
\code{method = "wald"} and \code{se_at = "compatible"} with
\code{focus_engine_list} objects, these are passed to \code{V_function}.}
}
\value{
A numeric vector of length 2 with names \code{"lower"} and \code{"upper"}.
}
\description{
Compute confidence intervals for objects returned by \code{\link[=focus]{focus()}}.
}
\details{
For \code{method = "wald"}, the interval is computed from the stored point
estimate and a delta-method standard error using the normal approximation.
By default, the stored standard error is used. If \code{se_at = "compatible"},
\code{focus_se()} is used to compute a compatible standard error lazily. If that
computation fails, a warning is issued and the stored standard error is used.

For \code{method = "pl"}, \code{\link[=profile_ci]{profile_ci()}} is used to compute a likelihood
profile interval for the scalar parameter defined by the stored \code{on}
function. If the stored fitted object is not an ML fit, it is refitted by
maximum likelihood before profiling. This interval is likelihood-based and
does not use the bias-corrected focus estimate as the likelihood centre.
Profile intervals are currently not available for \code{focus_engine()} results.

For \code{method = "hulc"}, \code{\link[=hulc_ci]{hulc_ci()}} is applied to data recovered from the
stored fitted object using \code{\link[=focus_statistic]{focus_statistic()}} as the statistic evaluated
on each partition. The recovered data must be suitable for refitting the
stored fitted object with \code{\link[stats:update]{stats::update()}}. HulC intervals are currently
not available for \code{focus_engine()} results.

The nominal coverage level is determined by \code{level}; users should
not supply \code{level} in \code{...}.
}
\seealso{
\code{\link[=focus]{focus()}}, \code{\link[=hulc_ci]{hulc_ci()}}, \code{\link[=focus_statistic]{focus_statistic()}}
}
